# 🧪 小窗 XIAOCHUANG - 产品体验与测试评审报告

> **评审日期**: 2026-07-13
> **当前版本**: 0.23.2（画布可信闭环验收版）
> **下一版本**: 0.24.0（短剧项目中枢升级版）
> **评审方法**: 代码静态分析 + 架构审查 + 组件与页面交互逻辑审查 + 数据库模型审查 + 已有测试证据审查
> **评审视角**: 产品体验专家 × 软件测试工程师 × 用户研究人员

> **注意**: 本次评审主要基于代码静态分析（code review）和架构审查。部分交互层问题（如运行时性能、API 超时、真实网络环境异常）需要在本地启动完整环境后进行补充验证。

---

## 📋 目录

1. [产品定位与核心目标](#1-产品定位与核心目标)
2. [用户体验流程](#2-用户体验流程)
3. [功能完整性测试](#3-功能完整性测试)
4. [交互与界面体验](#4-交互与界面体验)
5. [异常与边界场景](#5-异常与边界场景)
6. [性能与稳定性](#6-性能与稳定性)
7. [业务逻辑与数据一致性](#7-业务逻辑与数据一致性)
8. [安全与权限体验](#8-安全与权限体验)
9. [问题清单](#9-问题清单)
10. [总体评价与优化建议](#10-总体评价与优化建议)

---

## 1. 产品定位与核心目标

### 1.1 软件主要解决什么问题

小窗 XIAOCHUANG 试图解决 **AI 短剧与视频生产全链路工程化** 的问题。核心痛点：

- 从创意到成片链路太长、环节太碎
- 文案、剧本、分镜、素材、任务状态彼此割裂
- AI 能力多但缺少统一编排、统一状态、统一资产沉淀
- 团队协作时流程容易失控，复用很难发生

**一句话定位**: 把「灵感 → 剧本 → 分镜 → 素材 → 成片」做成一条真正可工程化、可复用、可共建的生产链路。

### 1.2 目标用户

| 角色 | 需求 | 当前覆盖 |
|------|------|----------|
| **内容创作者/编剧** | 快速将创意推进为结构化剧本和分镜 | ✅ 短剧AI-first管线、写作工作室 |
| **视频制作人员** | 从分镜到镜头生产到成片合成 | ✅ 分镜生成、镜头生产、视频合成导出 |
| **AIGC 工程师** | 调试模型、编排工作流 | ✅ 画布DAG编辑器、AI配置、Agent配置 |
| **运营/管理者** | 查看内容、用户、订阅状态 | ⚠️ 管理后台仅只读，无操作能力 |
| **产品经理** | 验证AI工作流链路 | ✅ 完整核心链路可验证 |

### 1.3 核心使用场景

1. **快速生成**: 从首页输入框快速生成图片/视频/音频 → ✅ 清晰
2. **短剧制作**: 源稿输入 → AI分析 → 改编策略 → 分集蓝图 → 剧本正文 → 分镜 → 镜头生产 → 合成导出 → ✅ 完整但复杂
3. **小说/剧本写作**: 设定准备 → 大纲 → 章节写作 → 审阅 → 导出 → ✅ 清晰
4. **画布编排**: 节点可视化编排AI生成管线 → ✅ 适合高级用户

### 1.4 产品定位支撑度评估

| 维度 | 评分 | 说明 |
|------|------|------|
| 问题定义清晰度 | ⭐⭐⭐⭐⭐ | README 和版本规划文档极其清晰 |
| 目标用户匹配度 | ⭐⭐⭐⭐ | 创作者和工程师两端的体验差异较大 |
| 核心链路完整度 | ⭐⭐⭐⭐ | 短剧链路完整但路径较长 |
| 差异化竞争力 | ⭐⭐⭐⭐ | 工程化全链路 vs 单点AI工具的定位差异化明确 |
| 版本规划合理性 | ⭐⭐⭐⭐⭐ | VERSION_PLAN.md 版本口径、做/不做范围极其清晰 |

---

## 2. 用户体验流程

### 2.1 用户首次进入体验

**流程路径**: 访问网站 → 登录页 → 注册/登录 → 首页 → 选择功能入口

```
访问首页 → 中间件检测无 session cookie → 重定向到 /login?next=/
  → 看到登录页（手机号+验证码 / 密码登录 双Tab）
  → 开发环境: 任意手机号 + 验证码 123456 登录
  → 登录成功 → 设置 xiaochuang_session cookie → 重定向到首页
```

**优点**:
- 登录页设计精美，品牌感强
- 双Tab切换（验证码/密码）满足不同场景
- 手机号输入自动去除非数字字符、限制11位，交互防呆做得好
- 支持 `?next=` 参数，登录后回到目标页面
- 开发环境有 `DEV_AUTH_BYPASS` 和 mock 验证码，开发体验好

**可改进**:
- 未登录用户无法看到任何产品内容（产品展示/landing page），直接跳登录可能流失潜在用户
- 验证码冷却60秒无动画进度反馈（仅有文字倒计时）
- 无第三方登录（微信/GitHub/Google），对创作者不够友好

### 2.2 首页体验

**首页结构**: Hero区域 + InputComposer（快速生成入口）+ 三大功能卡片（短剧/写作/画布）+ 最近项目列表

```
首页 (/)
├── 品牌标语 "透过小窗，发现生活的美"
├── InputComposer（模式切换: 图片/视频/音频）
│   ├── 图片模式: 模型选择 + 比例 + 2K/4K + 参考图上传
│   ├── 视频模式: 模型选择 + 首尾帧 + 比例 + 时长
│   └── 音频模式: 文本输入 + 音色选择 + 语速/情感
├── 快捷入口卡片: 新建短剧 / 小说剧本 / 智能画布
└── 最近更新的项目列表（带hover预览）
```

**优点**:
- InputComposer 多模式切换流畅，单一入口承载三种AI能力
- 快捷入口卡片清晰，降低选择成本
- 最近项目支持 hover 预览，降低了重复进入的成本

**可改进**:
- InputComposer 功能非常多（模型、比例、分辨率、参考图、首尾帧、音色、语速...），新用户可能不知所措
- 无新手引导/教程入口
- "最近项目"列表在首次使用时只是EmptyState，缺少引导性CTA

### 2.3 短剧 AI-First 管线体验

**完整路径**: 创建短剧 → 输入源稿 → 源稿健康检查 → AI分析 → 改编策略 → 分集蓝图 → 试播剧本正文 → 故事图谱 → 单集工作台 → 剧本编辑 → 分镜拆分 → 首帧生成 → 配音 → 视频生成 → 合成导出

**路径长度评估**:

| 阶段 | 步骤数 | 用户操作 | AI耗时 | 体验 |
|------|--------|----------|--------|------|
| 源稿理解 | 3 | 粘贴源稿 → 健康检查 → 确认分析 | 取决于源稿长度 | ⚠️ 较长 |
| 改编策略 | 2 | 查看策略 → 选择方向 | 中等 | ✅ |
| 分集蓝图 | 2 | 触发生成 → 确认蓝图 | 中等 | ✅ |
| 试播剧本 | 2 | 触发生成 → 查看正文 | 较长 | ⚠️ |
| 故事图谱 | 1 | 构建图谱 → 搜索 | 中等 | ✅ |
| 单集工作台 | 5+ | 剧本编辑 → 分镜 → 首帧 → 配音 → 视频 → 合成 | 非常长 | ⚠️⚠️ |

**核心问题**:
- 从源稿到第一个可看的视频，路径非常长（10+步骤），用户容易在中途流失
- 每个AI步骤都可能失败，失败后的恢复体验是薄弱环节
- 管线进度不可视，用户不知道自己在整体管线中的位置

### 2.4 写作工作室体验

**流程**: 创建写作项目 → Step1 设定 → Step2 大纲 → Step3 章节写作 → Step4 审阅 → Step5 导出

**优点**:
- 五步向导式流程清晰，有步骤进度指示
- AI聊天助手侧面板实时可用
- 知识卡片和参考网络可视化增强理解

**可改进**:
- 流程较长，缺少"跳过/快速模式"
- Step 之间没有明确的"完成条件"，何时可以进入下一步不清晰

### 2.5 画布编辑器体验

**场景**: 高级用户的可视化AI管线编排

**优点**:
- React Flow 实现的节点编辑器专业
- 支持内容节点 + 执行节点 + 数据流边 + 叙事边
- 生成历史、资产入资产库、来源追溯完成度高

**可改进**:
- 学习曲线陡峭，非技术用户难以理解DAG概念
- 空白画布的引导（CanvasEmptyState）虽好，但缺少模板/预设

### 2.6 用户独立完成任务能力评估

| 任务 | 不看说明能完成吗？ | 障碍 |
|------|---------------------|------|
| 注册/登录 | ✅ 能 | 基本无 |
| 快速生成图片 | ✅ 能 | 模式切换直观 |
| 快速生成视频 | ⚠️ 部分 | 首尾帧、参考模式概念需要理解 |
| 创建短剧项目 | ✅ 能 | 创建对话框简洁 |
| 完成AI-first管线 | ⚠️ 困难 | 步骤多、AI失败处理、术语专业 |
| 写作创作 | ✅ 能 | 向导式引导好 |
| 使用画布 | ❌ 困难 | 需要DAG/节点编辑概念 |

---

## 3. 功能完整性测试

### 3.1 功能模块总览

| 模块 | 状态 | 完整度 | 说明 |
|------|------|--------|------|
| **用户认证** | ✅ 可用 | 90% | 手机验证码+密码登录+注册，缺第三方登录 |
| **首页/快速生成** | ✅ 可用 | 85% | 图片/视频/音频三模式，缺批量操作引导 |
| **短剧项目** | ✅ 可用 | 80% | CRUD + AI-first管线 + 单集工作台，路径长 |
| **短剧AI-first管线** | ✅ 可用 | 75% | 源稿→策略→蓝图→剧本→图谱，部分步骤AI依赖强 |
| **单集工作台** | ✅ 可用 | 80% | 剧本+分镜+制作+导出，镜头生产环节待完善 |
| **写作工作室** | ✅ 可用 | 85% | 5步向导+AI助手，审阅与知识图谱有价值 |
| **画布编辑器** | ✅ 可用 | 80% | DAG节点编辑+生成+历史+资产闭环，学习曲线陡 |
| **资产库** | ✅ 可用 | 80% | 按类型筛选+回源画布，缺批量管理 |
| **管理后台** | ⚠️ 仅只读 | 40% | 可查看用户/短剧/订阅，无任何编辑操作能力 |
| **设置中心** | ✅ 可用 | 80% | AI配置+Agent配置+Skills+偏好设置 |
| **用户中心** | ✅ 可用 | 60% | 基础信息+统计，功能较薄 |
| **任务系统** | ✅ 可用 | 85% | 异步任务+队列+重试+取消，后端能力强 |

### 3.2 功能闭环检查

| 闭环 | 状态 | 断裂点 |
|------|------|--------|
| 灵感 → 快速生成图片/视频/音频 | ✅ 闭环 | 产物自动入资产，可回源 |
| 小说 → 改编策略 → 分集 → 剧本 | ✅ 闭环 | 需要每个环节AI成功 |
| 剧本 → 分镜 → 首帧 → 视频 → 合成 | ⚠️ 部分闭环 | 视频生成→合成路径长，中间失败恢复机制薄弱 |
| 画布 → 生成 → 历史 → 资产 | ✅ 闭环 | 0.23.2重点验证通过 |
| 写作 → 审阅 → 导出 → 导入短剧 | ⚠️ 部分闭环 | 导入短剧的体验未在版本中明确验证 |
| 管理后台 → 查看 → 编辑 → 生效 | ❌ 未闭环 | 管理后台无编辑能力 |
| 设置AI配置 → 使用AI → 消耗配额 | ⚠️ 部分闭环 | 配额统计与消耗追踪不透明 |

### 3.3 CRUD 操作检查

| 功能 | 创建 | 读取 | 更新 | 删除 | 状态 |
|------|------|------|------|------|------|
| 短剧项目 | ✅ | ✅ | ✅ (标题等基础) | ✅ (软删除) | 基本完整 |
| 分集 | ✅ | ✅ | ✅ | ⚠️ 未见明显入口 | 可补充 |
| 写入项目 | ✅ | ✅ | ✅ | ⚠️ 未见明显入口 | 可补充 |
| 画布 | ✅ | ✅ | ✅ (标题) | ✅ (软删除) | 基本完整 |
| 角色 | ✅ | ✅ | ✅ | ⚠️ 未见明显入口 | 可补充 |
| 场景 | ✅ | ✅ | ✅ | ⚠️ 未见明显入口 | 可补充 |
| AI配置 | ✅ | ✅ | ✅ | ✅ | 完整 |
| Agent配置 | ✅ | ✅ | ✅ | ✅ | 完整 |
| Skills | ✅ | ✅ | ✅ | ✅ | 完整 |

### 3.4 缺失功能识别

1. **管理后台编辑能力** (P1): 无法通过后台禁用用户、审核短剧、管理订阅
2. **批量操作** (P2): 无法批量删除/导出短剧、批量生成镜头
3. **回收站** (P2): 代码提到"删除后进入回收站，30天可恢复"，但未见回收站页面
4. **通知系统** (P2): 任务完成、审核状态变更无站内通知
5. **第三方登录** (P2): 无微信/GitHub/Google登录
6. **协作功能** (P3): 版本计划明确不做，但多用户短剧项目编辑无支撑
7. **模板/预设** (P2): 画布和短剧创建无模板，新用户入门成本高
8. **使用统计/配额仪表盘** (P2): 用户无法查看自己的API用量和配额剩余

---

## 4. 交互与界面体验

### 4.1 页面布局与信息层级

**web 应用**:
- 整体布局: 左侧Sidebar + 顶部Header + 主体内容区（default layout）
- 工作室模式: 全屏沉浸式（studio layout），无全局Sidebar
- 信息层级: 页面标题 → 工具栏/筛选 → 内容区（卡片/表格）→ 详情/操作
- 响应式断点: sm(640px) / md(768px) / lg(1024px) / xl(1280px)

**admin 应用**:
- 左侧暗色Sidebar + 顶栏 + 主内容区
- 独特的暖大地色设计系统，与web端风格区分

**评价**: 布局清晰，信息层级合理。studio 模式的全屏设计很好地服务于沉浸式创作需求。

### 4.2 文案质量

| 场景 | 文案 | 评价 |
|------|------|------|
| 登录页标题 | "透过小窗，发现生活的美" | ✅ 品牌感强 |
| 错误信息 | "请输入有效的中国大陆手机号" | ✅ 具体明确 |
| 空状态 | "还没有短剧项目" / "创建你的第一部 AI 辅助写作作品。" | ✅ 友好有引导 |
| 确认对话框 | "删除后项目将进入回收站，30 天内可恢复。确认删除？" | ✅ 信息充分 |
| AI错误映射 | `getAiErrorCopy()` 将技术错误码映射为中文 | ✅ 用户体验好 |
| 网络错误 | `friendlyFetchErrorMessage()` 替换"Failed to fetch" | ✅ 可理解性强 |

### 4.3 UI 状态覆盖

| 状态类型 | 覆盖情况 | 示例 |
|----------|----------|------|
| **加载中** | ✅ 完善 | Skeleton shimmer动画 + Loader2 spinner + 保存状态指示器 |
| **空状态** | ✅ 完善 | EmptyState组件 + CanvasEmptyState + 步骤级空态 |
| **错误状态** | ✅ 良好 | error.tsx边界 + toast通知 + 内联错误横幅 + AI错误映射 |
| **404状态** | ✅ 良好 | 品牌化not-found页面 + 返回首页链接 |
| **禁用状态** | ⚠️ 部分 | 按钮有loading disabled，但表单字段禁用态覆盖不足 |
| **成功反馈** | ✅ 良好 | toast.success() 在关键操作后调用 |
| **进度状态** | ⚠️ 部分 | AI任务有RunProgressIndicator，但管线级进度不可见 |

### 4.4 视觉一致性

**优点**:
- CSS变量系统统一（`--color-accent`, `--color-bg-0`, `--color-text-0`等）
- 组件库（ContentKit: ContentSurface, ContentPageHeader, ContentToolbar等）确保列表页风格统一
- 卡片hover效果一致（`group-hover:scale-[1.025]` 等微交互）
- admin 端有独立但协调的设计系统

**可改进**:
- web端部分地方使用原生 `window.confirm()` 而非自定义 ConfirmDialog，风格不一致（如画布列表页）
- 部分空状态使用内联 `<div>` 而非共享 EmptyState 组件（如script-panel中的step-empty）
- admin 端与 web 端的视觉风格差异较大（暖大地色 vs 冷色调），可考虑品牌统一

### 4.5 移动端适配

**适配情况**: 使用 Tailwind 响应式断点
- 卡片网格: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4`
- 文本尺寸: `text-lg sm:text-xl` 等层级响应
- 间距: `p-3 sm:p-4 lg:p-5` 递进
- 按钮: 移动端全宽 `w-full`，桌面端 `sm:w-auto`
- 搜索栏: 移动端堆叠，桌面端 `sm:flex`

**可改进**: 画布编辑器在移动端几乎不可用（React Flow 节点编辑不适合小屏），需要至少给出提示或降级方案。

---

## 5. 异常与边界场景

### 5.1 输入校验

| 场景 | 当前处理 | 评价 |
|------|----------|------|
| 手机号非数字 | `replace(/\D/g, '')` 自动过滤 + 截断11位 | ✅ 防呆好 |
| 验证码非6位数字 | `replace(/\D/g, '').slice(0, 6)` + 正则校验 | ✅ |
| 密码为空 | 前端检查 `!normalizedPassword` | ✅ |
| 密码强度 | **无**最小长度以外的复杂度要求 | ⚠️ 可加强 |
| 超长文本输入 | HTML `maxLength` 仅部分字段设置 | ⚠️ 不一致 |
| Email格式 | 仅HTML `type="email"` 检查 | ⚠️ 弱校验 |
| SQL注入/XSS | 后端使用Drizzle ORM参数化查询，前端React自动转义 | ✅ |
| 表单重复提交 | 按钮 `disabled` + `loading` 状态防抖 | ✅ |

### 5.2 网络异常处理

| 场景 | 当前处理 | 评价 |
|------|----------|------|
| API请求失败 | `try/catch` + `toast.error()` 显示错误信息 | ✅ |
| fetch网络错误 | `friendlyFetchErrorMessage()` 将"Failed to fetch"替换为中文 | ✅ |
| Session过期 | API 401自动重定向到 `/login?next=` | ✅ |
| 后端不可达 | Next.js API proxy返回500，前端toast提示 | ⚠️ 信息可更具体 |
| 长时间无响应 | **未见**明确超时设置 | ⚠️ 需要补充 |
| 无网络时访问 | middleware重定向到login（无session cookie） | ⚠️ 无离线提示 |

### 5.3 权限与访问控制

| 场景 | 当前处理 | 评价 |
|------|----------|------|
| 未登录访问受保护页 | middleware 检测cookie → 重定向到login | ✅ |
| 普通用户访问管理后台 | `getAdminSession()` 检查 role 不是 admin/super_admin → 重定向 | ✅ |
| 跨用户数据访问 | 后端查询带 `userId` 过滤 | ✅ |
| 直接调用后端API越权 | `SessionAuthGuard` + `@Roles()` 装饰器 | ✅ |
| API proxy层权限 | **无**额外校验，依赖后端 | ⚠️ 可加强 |

### 5.4 并发与数据一致性

| 场景 | 当前处理 | 评价 |
|------|----------|------|
| 重复点击提交 | 按钮 disabled + loading | ✅ |
| 画布并发生成 | 0.23.2 引入行锁 `SELECT FOR UPDATE` | ✅ |
| 多个Tab同时操作 | **未见**跨Tab状态同步机制 | ⚠️ |
| 删除后恢复 | drama 使用软删除 `deletedAt` | ✅（但无回收站UI） |
| 删除关联数据 | 级联删除未见明确处理 | ⚠️ 需验证 |

### 5.5 文件上传

| 场景 | 当前处理 | 评价 |
|------|----------|------|
| 图片上传 | 30MB限制，前端验证 | ✅ |
| 视频上传 | 200MB限制，前端验证 | ✅ |
| 音频上传 | 100MB限制，前端验证 | ✅ |
| 非图片文件上传为参考图 | **未见**文件类型校验 | ⚠️ |
| 超大文件 | 后端应有验证（代码未完全审查） | ⚠️ 需验证 |
| 上传失败重试 | **未见**自动重试机制 | ⚠️ |

---

## 6. 性能与稳定性

### 6.1 架构级性能措施

| 措施 | 实现 | 评价 |
|------|------|------|
| 服务端渲染 | Next.js App Router Server Components 用于数据获取 | ✅ |
| 客户端缓存 | `lib/api.ts` 30秒内存缓存 + 请求去重 | ✅ |
| 代码分割 | `dynamic(() => import(...))` 用于大组件（画布编辑器等） | ✅ |
| 图片优化 | Next.js Image组件 + 静态资源URL | ✅ |
| 骨架屏 | loading.tsx + SkeletonGrid + ContentGridSkeleton | ✅ |
| SSE流式 | AI运行时支持SSE流式响应 | ✅ |

### 6.2 潜在性能风险

| 风险 | 说明 | 严重程度 |
|------|------|---------|
| 源稿长文本处理 | 长篇小说的token估算、分块处理可能超时 | P1 |
| 画布大量节点 | React Flow 在100+节点时可能出现卡顿 | P2 |
| 资产列表分页 | assets页面未见分页，资产量大时可能加载缓慢 | P2 |
| 首页InputComposer | 同时加载多个模型列表、音色列表 | P3 |
| API proxy 性能 | 所有请求通过Next.js API route代理，增加一跳延迟 | P2 |

### 6.3 稳定性评估

| 风险 | 等级 | 说明 |
|------|------|------|
| 未捕获异常导致白屏 | Low | 有 error.tsx 和 global-error.tsx 边界 |
| 内存泄漏 | Low-Medium | Zustand store + 事件监听器清理需要逐一验证 |
| 后端崩溃恢复 | Low | BullMQ 支持任务持久化和重试 |
| 数据库连接耗尽 | Low | Drizzle ORM 连接池管理 |
| 第三方AI服务不稳定 | Medium | 依赖外部AI服务，部分provider可能不稳定 |

---

## 7. 业务逻辑与数据一致性

### 7.1 数据流一致性

| 操作 | 前端状态更新 | 后端持久化 | 前后端一致 | 评价 |
|------|-------------|-----------|-----------|------|
| 创建短剧 | 乐观更新（列表刷新） | INSERT + 返回新记录 | ✅ | 标准模式 |
| 删除短剧 | 刷新列表 | 软删除（deletedAt） | ✅ | 30天可恢复 |
| AI生成结果 | 任务轮询/SSE | 任务状态机 + 结果回写 | ✅ | 异步模式正确 |
| 画布节点修改 | Zustand store 即时更新 | 后端保存 | ⚠️ | 自动保存时机未知 |
| 分镜草稿→正式 | publish状态切换 | 状态字段更新 | ⚠️ | 并发编辑需验证 |

### 7.2 状态流转

**短剧状态**: `draft` → `published`（未见完整状态机定义）

**审核状态**: `pending` → `approved` / `rejected`（后端字段完整，管理后台只读）

**订阅状态**: `active` / `cancelled` / `expired`

**任务状态**: 队列任务有完整的 pending → active → completed/failed 状态流转

### 7.3 软删除策略

- `users`: `deletedAt`（软删除）
- `dramas`: `deletedAt`（软删除，30天可恢复）
- `agentConfigs`: `deletedAt`（软删除）
- `organizations`: `deletedAt`（软删除）

**问题**: 软删除后关联数据（分集、分镜、角色等）的处理策略不清晰。

---

## 8. 安全与权限体验

### 8.1 认证安全

| 措施 | 实现 | 评价 |
|------|------|------|
| 密码加密 | `password.ts` 使用 `crypto.pbkdf2Sync` (SHA-256, 100000+迭代) | ✅ 良好 |
| Session管理 | httpOnly cookie + 7天过期 + token hash存储 | ✅ 良好 |
| Session撤销 | `authSessions.revokedAt` 字段支持 | ✅ |
| SIM卡验证码 | 60秒冷却 + 5次尝试限制 + 过期时间 | ✅ |
| 暴力破解防护 | **未见**登录失败次数限制/IP限流 | ⚠️ 需要补充 |
| CSRF防护 | **未见**CSRF token机制 | ⚠️ 依赖SameSite cookie |

### 8.2 API密钥安全

**⚠️ 重要风险**: AI服务配置中的 `apiKey` 以**明文文本**存储在 `ai_service_configs` 表中（`apiKey: text("api_key").notNull()`）。

这是典型的安全隐患：
- 数据库泄露 = 所有用户API密钥泄露
- 无加密存储、无密钥轮换机制
- 前端API返回中是否过滤 `apiKey` 字段需要验证

### 8.3 权限模型

```
角色层级: super_admin > admin > user
权限检查: @Roles('admin', 'super_admin') + SessionAuthGuard
管理后台: getAdminSession() 硬编码检查 role === 'admin' || 'super_admin'
```

**评价**: 基本的RBAC模型存在，但：
- 权限定义较为粗糙（三个角色，permission字段为text存储但未见细粒度使用）
- 无资源级权限（如：只能查看/编辑自己创建的短剧）
- 前端路由保护通过middleware + cookie存在性判断，未验证角色

### 8.4 数据隔离

- 后端API查询时通过 `userId` 过滤（从session中获取）
- 管理后台通过 `/api/v1/admin/*` 独立路径隔离
- 跨用户数据访问通过 `SessionAuthGuard` 阻止

---

## 9. 问题清单

| 编号 | 问题类型 | 问题描述 | 复现路径 | 严重程度 | 影响范围 | 优化建议 |
|------|----------|----------|----------|----------|----------|----------|
| P-01 | 安全 | AI服务API密钥明文存储在数据库中 | 查看 `ai_service_configs.api_key` 字段设计 | **P1** | 所有配置了AI服务的用户 | 使用 AES-256-GCM 加密存储，密钥从环境变量/密钥管理服务获取 |
| P-02 | 安全 | 无登录失败次数限制和IP限流 | 对 `/api/v1/auth/login/*` 接口进行暴力破解 | **P1** | 所有用户账户安全 | 添加登录失败计数器和IP限流（如：5次失败后锁定15分钟） |
| P-03 | 功能缺失 | 管理后台仅只读，无任何编辑/管理操作 | 进入 `/dashboard/users` → 只能"查看"，无禁用/编辑/删除按钮 | **P1** | 平台运营管理能力 | 添加用户禁用/启用、短剧审核通过/驳回、订阅管理操作 |
| P-04 | 用户体验 | 短剧AI-first管线路径过长，用户容易流失 | 创建短剧 → 源稿 → 分析 → 策略 → 蓝图 → 剧本 → 分镜 → 首帧 → 视频 → 合成 | **P1** | 短剧创作核心用户 | 添加"快速模式"跳过部分步骤；显示管线总进度条；允许并行执行独立步骤 |
| P-05 | 功能缺失 | 无回收站页面，"删除后30天可恢复"无法操作 | 删除一个短剧 → 无回收站入口 → 无法恢复 | **P2** | 误删除用户 | 实现回收站页面，支持恢复和彻底删除 |
| P-06 | 功能缺失 | 无站内通知系统 | AI任务完成 → 无通知 → 用户需要手动刷新查看状态 | **P2** | 所有使用AI功能的用户 | 添加通知中心（任务完成、审核状态变更等），支持浏览器通知 |
| P-07 | 用户体验 | 画布编辑器学习曲线陡峭，新用户难以上手 | 进入 `/canvas` → 创建画布 → 面对空白DAG编辑器 | **P2** | 画布功能使用率 | 提供预设模板、交互式教程、节点推荐 |
| P-08 | 用户体验 | 无第三方登录方式 | 登录页 → 仅手机验证码/密码两种方式 | **P2** | 新用户注册转化率 | 添加微信扫码登录、GitHub/Google OAuth |
| P-09 | 数据一致性 | 软删除后关联数据处理策略不明确 | 删除短剧 → 关联的分集/分镜/角色/场景的数据状态未知 | **P2** | 数据一致性和存储 | 明确级联软删除策略，或在删除确认中说明关联数据处理方式 |
| P-10 | 用户体验 | 密码强度校验不足 | 注册 → 输入密码 "12345678"（8位纯数字） → 通过 | **P2** | 账户安全 | 增加密码复杂度要求（至少包含字母+数字，或使用zxcvbn等库评估强度） |
| P-11 | 界面体验 | 部分删除操作使用原生window.confirm，风格不统一 | 画布列表页 → 点击删除 → 浏览器原生弹窗 | **P3** | 视觉一致性 | 统一使用ConfirmDialog组件替代所有原生弹窗 |
| P-12 | 功能缺失 | 无批量操作能力 | 短剧列表 → 无多选 → 无法批量删除/导出 | **P3** | 高级用户效率 | 添加多选模式，支持批量删除、批量导出 |
| P-13 | 用户体验 | AI管线失败后恢复体验不佳 | AI分集蓝图生成失败 → 需要手动重试 → 重试可能再次失败 | **P1** | 短剧AI-first管线所有用户 | 提供失败原因分析、部分结果保留、智能重试建议 |
| P-14 | 性能 | 长篇小说源稿可能导致token溢出或超时 | 粘贴10万字小说 → 健康检查 → token估算 → 分块处理超时 | **P2** | 使用长篇源稿的用户 | 添加源稿长度预警、分块处理进度、支持断点续传 |
| P-15 | 功能缺失 | 用户无API使用量/配额可视化 | 设置 → AI配置 → 无使用量统计 | **P2** | 关注成本的用户 | 添加配额仪表盘，显示视频/图片/音频/token的用量和剩余 |
| P-16 | 安全 | API proxy层无额外安全校验 | `/api/v1/[[...path]]` 直接转发所有请求到后端 | **P3** | 安全防护层 | 在proxy层添加基础请求校验、敏感字段过滤 |
| P-17 | 界面体验 | 部分空状态使用内联样式，而非统一的EmptyState组件 | script-panel中的step-empty使用div+class | **P3** | 维护性和一致性 | 统一使用EmptyState组件，支持更丰富的空状态变体 |
| P-18 | 可用性 | 画布编辑器在移动端完全不可用 | 手机浏览器访问 `/canvas/[id]` → 节点无法操作 | **P2** | 移动端用户 | 移动端至少显示"请在桌面端使用"的提示，或提供简化只读模式 |
| P-19 | 功能缺失 | 设置页无AI配置连通性测试 | 添加OpenAI API Key → 保存 → 无测试按钮 → 使用时才发现配置错误 | **P2** | 配置AI服务的用户 | 在AI配置表单中添加"测试连接"按钮 |
| P-20 | 数据一致性 | 多个浏览器Tab同时操作无同步机制 | Tab1修改短剧 → Tab2也打开同一短剧进行修改 → 数据覆盖 | **P3** | 多Tab使用场景 | 使用BroadcastChannel API或定时轮询实现跨Tab状态同步 |

---

## 10. 总体评价与优化建议

### 10.1 综合评分

| 维度 | 评分 | 权重 | 加权分 |
|------|------|------|--------|
| 产品定位与核心目标 | 9.0/10 | 15% | 1.35 |
| 用户体验流程 | 7.0/10 | 20% | 1.40 |
| 功能完整性 | 7.5/10 | 20% | 1.50 |
| 交互与界面体验 | 8.0/10 | 15% | 1.20 |
| 异常与边界处理 | 6.5/10 | 10% | 0.65 |
| 性能与稳定性 | 7.5/10 | 5% | 0.38 |
| 业务逻辑与数据一致性 | 7.5/10 | 10% | 0.75 |
| 安全与权限 | 6.0/10 | 5% | 0.30 |

> **综合评分: 7.5 / 10**

### 10.2 最值得保留的优点

1. **清晰的产品定位与版本规划**: README和VERSION_PLAN.md是见过最清晰的版本规划和产品定位文档之一。做/不做的范围极其明确，版本口径统一，技术/产品视角兼顾。

2. **全链路工程化能力**: 从灵感输入、AI分析、剧本生成、分镜拆分、镜头生产到合成导出，是一条真正可运行的端到端链路，不是单点Demo。这在同类开源项目中非常罕见。

3. **高质量的UI工程**: 自定义设计系统、完善的Empty/Loading/Error三态覆盖、CSS变量体系、响应式适配、骨架屏、ARIA无障碍标注、错误信息中文化等，体现了良好的前端工程素养。

4. **清晰的架构边界**: Monorepo 中 web/admin/backend/contracts/canvas-shared/ui 的分层清晰，契约层（contracts）和画布共享层（canvas-shared）的设计体现了良好的工程思维。

5. **0.23.2 画布闭环的工程严谨性**: 从并发生成行锁、资产来源追溯、生成历史、上传限制到E2E测试的完整闭环，展现了团队在"把功能做可靠"方面的工程能力。

### 10.3 最需要优先修复的 3 个问题

1. **P-01: AI API密钥明文存储** (P1-安全)
   - 影响所有配置了AI服务的用户
   - 数据库泄露 = API密钥泄露 = 用户AI服务被盗用
   - 修复成本中等：引入加密层，对 `apiKey` 字段进行 AES-256-GCM 加密存储

2. **P-03: 管理后台无编辑能力** (P1-功能缺失)
   - 影响平台运营能力
   - 无法通过后台处理用户问题、审核内容、管理订阅
   - 修复成本中等：为admin添加用户禁用/启用、短剧审核、订阅管理API和UI

3. **P-13: AI管线失败后恢复体验** (P1-用户体验)
   - 影响短剧AI-first管线所有用户
   - AI失败后用户茫然，缺乏清晰的恢复指引
   - 修复成本中等：为每个管线步骤添加失败原因分类、部分结果保留、一键重试和跳过选项

### 10.4 下一版本最应该优化的方向

**优先级最高**: 短剧AI-first管线的体验优化（与0.24.0方向一致）
- 管线进度可视化（用户知道自己在哪一步，还剩几步）
- 快速模式/跳过选项（有经验的用户不需要每一步确认）
- AI步骤可并行执行（分析源稿的同时可以生成改编策略）
- 失败恢复体验（部分结果不丢失、智能重试、失败原因可读）

**优先级次高**: 安全加固
- API密钥加密存储
- 登录限流
- 密码强度要求

**优先级再次**: 管理后台操作能力
- 用户管理（禁用/启用/删除）
- 内容审核（短剧审核通过/驳回）
- 订阅管理

### 10.5 上线建议

**不建议在当前状态下作为正式商用产品上线。** 原因如下：

1. **安全风险**: API密钥明文存储是上线前的阻断性问题。一旦发生数据泄露，直接影响所有用户的第三方AI服务安全。

2. **管理能力不足**: 管理后台仅只读意味着无法处理用户问题、无法审核内容、无法管理订阅。这对于一个需要运营的平台是不可接受的。

3. **AI管线体验不够稳定**: 短剧AI-first管线虽然是核心差异化功能，但路径过长、失败恢复机制薄弱，可能在真实用户场景中导致高流失率。

**建议上线路径**:

1. **第一阶段（安全加固 + 管理能力）**: 修复API密钥加密存储、添加管理后台编辑能力 → 内部Alpha测试
2. **第二阶段（管线体验优化）**: 短剧管线进度可视化、快速模式、失败恢复 → 邀请制Beta测试
3. **第三阶段（补充功能）**: 通知系统、回收站、第三方登录 → 公开Beta → 正式上线

**当前更适合的状态**:
- ✅ 作为开源项目展示和技术验证
- ✅ 作为内部工具/个人使用
- ✅ 作为AIGC工作流的技术底座（供开发者二开）
- ⚠️ 作为邀请制Beta产品（需要明确告知用户风险）
- ❌ 作为公开商用SaaS产品

---

## 附录

### A. 评审方法说明

本次评审采用以下方法组合：

1. **代码静态分析**: 审查了 web app、admin app、backend 的核心代码结构、组件实现、API路由、中间件、数据模型
2. **架构审查**: 评估了 Monorepo 结构、前后端分离模式、数据流设计、状态管理策略
3. **交互逻辑审查**: 通过代码审查追踪了关键用户流程的完整路径（登录→首页→短剧创建→AI管线→单集工作台→画布）
4. **组件审查**: 逐一审查了 EmptyState、ConfirmDialog、Skeleton、ErrorBoundary 等关键UI模式的实现
5. **已有测试证据审查**: 参考了 TEST_REPORT.md 中的 E2E、API端点、静态代码分析结果
6. **数据库模型审查**: 审查了 schema.ts 中的表结构、字段类型、索引和约束

### B. 未覆盖的测试场景

以下场景需要在本地启动完整环境后进行补充验证：

- [ ] 真实AI服务的调用成功率和响应时间
- [ ] 大数据量（100+短剧、1000+资产）下的列表加载性能
- [ ] 并发用户操作下的数据一致性
- [ ] 长篇小说（10万+字）的源稿分块处理
- [ ] 各浏览器（Chrome/Safari/Firefox/Edge）的兼容性
- [ ] 弱网环境下的体验（3G/4G限速）
- [ ] 画布编辑器在100+节点时的交互流畅度
- [ ] 视频合成在长视频（10分钟+）场景下的稳定性

---

> 📝 **评审人**: 产品体验专家 × 软件测试工程师 × 用户研究人员（AI辅助）
> 📅 **日期**: 2026-07-13
> 🔄 **下次评审建议**: 0.24.0 版本封版后
