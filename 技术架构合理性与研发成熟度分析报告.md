# 小窗 XIAOCHUANG — 技术架构合理性与研发成熟度分析报告

> 收口基线：产品版本 `0.22.0`｜代码收口 commit `ed34d09`（已完成 P0/P1-F/P1-G；文档提交另计）
> 评审范围：技术架构合理性 + 研发架构（工程化）成熟度
> 评审方法：仓库结构走读 + 关键模块逐文件阅读 + 依赖/脚本/迁移交叉验证
> 评审对象：全栈 TypeScript monorepo（`apps/web`、`apps/admin`、`apps/backend` + 3 个共享包）

---

## 0. 结论速览（TL;DR）

| 维度 | 评级 | 一句话判断 |
|---|---|---|
| **技术架构合理性** | **B+（良好）** | 分层清晰、边界正确、模式选型成熟；核心链路（契约优先 + 异步队列 + 领域策略）是工业级思路，外部基建被纳入正式运行边界，取舍合理。 |
| **研发架构成熟度** | **B-（可交付基线）** | 代码组织与本地验证脚本（30+ verify/audit gate）突出；CI、发布门禁、脚本校验、后端容器与运行时验证已经补齐，剩余短板主要是测试层级、统一 lint/pre-commit 与文档收敛。 |
| **综合** | **B（稳步上升）** | 一个"架构设计能力强、工程纪律正在固化"的高潜力项目。瓶颈已从"缺门禁"转为"提升门禁覆盖深度与长期维护纪律"。 |

**核心矛盾**：这是一个**设计意图（架构）很强，研发制度正在补齐**的项目。架构选型体现了资深工程师的判断力；本轮已把 CI/发布门禁/容器/运行时验证接上，后续重点是扩大测试覆盖、统一规范与文档单一真相源。

---

## 一、技术架构合理性分析

### 1.1 整体架构画像

```
┌─────────────┐   ┌─────────────┐
│  apps/web   │   │ apps/admin  │      前端双形态（创作端 / 管理端）
│ Next.js 15  │   │  Next.js    │      React 19 · Tailwind v4 · Zustand · @xyflow(画布)
└──────┬──────┘   └──────┬──────┘
       │  api/v1 catch-all proxy（同源代理，浏览器不直连后端）
       ▼                  ▼
┌───────────────────────────────────┐
│         apps/backend (NestJS 11)   │  Fastify · 27 业务模块 · DI 装配
│  auth│tasks│queue│ai│images│videos │  任务编排引擎 = 领域策略 + 注册表
│  audio│compose│merge│canvas│...     │
└───┬───────────┬───────────┬────────┘
    ▼           ▼           ▼
 PostgreSQL   Redis+BullMQ  S3/MinIO + FFmpeg/Sharp
 (Drizzle)    (异步任务)    (媒体产物 + 处理)

共享层：packages/contracts（OpenAPI→TS 类型，唯一契约源）
        packages/canvas-shared（画布节点/端口/执行语义）
        packages/ui（UI 边界）
```

规模事实：**~491 个 TS/TSX 源文件**（backend 204 / web 241 / admin 15 / canvas-shared 27），**48 张数据表**（schema.ts 1093 行），**27 个 NestJS 模块**，**8 个 Drizzle 迁移**。这是一个中等偏大的真实系统，不是 demo。

### 1.2 合理的架构决策（架构优点）

**① 契约优先（Contract-First）——最值得肯定的决策**
`apps/backend` 通过 `openapi:export` 导出 `openapi.json`，`packages/contracts` 用 `openapi-typescript` 生成类型，`web`/`admin` 只消费类型。README 明确"不放数据库连接/业务逻辑/鉴权/任何运行时真相源"。这条纪律把前后端类型漂移问题从根上消除，是很多团队做不到的成熟做法。

**② 前端零业务化 + 同源代理**
`apps/web/CLAUDE.md` 明确规定"web 仅面向用户，业务 API/schema/auth/queue/storage 编排必须留在 backend"。前端通过 `api/v1/[[...path]]` 单一 catch-all 代理转发后端，浏览器不直连后端服务。职责边界干净，安全面收敛。

**③ 异步任务编排：领域策略 + 注册表（教科书级）**
`tasks` 模块经过重构，抽出 `TaskDomainHandler` 接口 + `BaseTaskDomainHandler` 抽象基类 + 5 个领域 handler（image/video/tts/compose/merge）+ `TaskDomainRegistry`。`TasksService.retryTask/cancelTask` 退化为 3–5 行薄委托，新增领域只需注册一个 handler。这是标准的策略模式 + 注册表，消除了此前的 5×5 分发复制，扩展性好。

**④ 任务可靠性设计到位**
- 队列层 `enqueueTask` 用确定性 `jobId` + `getJob` 幂等去重，支持 `replaceExisting`；
- `attemptCount` 只在 worker `claimQueuedTask` 时自增（修复了手动重试双计数）；
- webhook 幂等：`isTerminalVideoGenerationStatus` 前置终态检查，防止重推 webhook 复活终态任务；
- 迁移 `0008_tasks_domain_active_unique` 用唯一约束保证同一领域实体同一时刻仅一个活跃任务；
- 独立 `worker-main.ts` + `worker.ts --once`（可做崩溃恢复/dry-run），任务执行与 API 进程分离。

这些是**"像系统一样能长"**的关键工程细节，说明作者理解分布式任务系统的真实痛点。

**⑤ 技术选型主流且自洽**
NestJS(DI/模块化) + Fastify(性能) + Drizzle(类型安全 SQL) + BullMQ(成熟队列) + S3 兼容存储(MinIO 本地验证) + Sharp/FFmpeg(媒体)。全链路 TypeScript，选型无明显反模式，学习曲线与社区支持良好。

**⑥ 画布执行语义下沉到共享包**
`canvas-shared` 把节点定义、端口类型、执行语义（text-to-image / image-to-video / tts / concat / export）抽成前后端共享层，并带单测（`node-definition.test.ts` / `port-type.test.ts`）。可视化编排与执行契约统一，避免前端画布与后端执行"各说各话"。

### 1.3 架构风险与不合理之处

| 级别 | 问题 | 说明与影响 |
|---|---|---|
| **已处理** | **媒体产物已移出 Git** | `apps/data/static` 已纳入忽略规则，历史跟踪的媒体产物已 `git rm --cached` 移除；当前 `git ls-files apps/data/static` 为 0。运行时产物边界已恢复。 |
| **P2** | **外部基建契约需显式化** | PG + Redis + S3/MinIO + FFmpeg 是项目正式组成部分，不需要另做一套降级实现。真正需要补的是部署契约、healthcheck/readiness、启动前诊断、错误提示与本地一键运行文档，确保缺组件时能快速定位而不是静默失败。 |
| **P2** | **admin 端明显欠发育** | admin 仅 15 个文件、目录只有 `app` + `lib`，与 web(241 文件) 严重不对称。README 承诺的"角色分工/多人协同/运营治理"在 admin 侧尚属骨架。 |
| **P2** | **前端引入两套 AI 运行时** | web 侧同时有 `@ai-sdk/openai` 与 `@mastra/core`，而后端也有完整 `ai` 模块。AI 能力在前后端都有落点，"统一编排"的边界需要警惕被稀释（AI 调用真相源应唯一）。 |
| **P2** | **鉴权为自研 Session + SMS 验证码** | `auth` 模块自研 session-guard/roles-guard/password/sms。功能完整，但自研鉴权的安全审计成本高；开发态"任意手机号 + 123456"便利的同时需确保生产严格隔离。 |
| **P3** | **README 与实现的版本标注不一致** | README 称 "Next.js 16"，实际 `next: ^15.5.15`；称 "NestJS 11 + Fastify" 属实。小瑕疵，但反映文档与代码存在漂移。 |

**架构合理性小结**：核心链路（契约优先 + 异步队列 + 领域策略 + 媒体流水线）设计合理且成熟，属于**可工程化、可扩展**的正确骨架。PG、Redis、S3/MinIO、FFmpeg 属于项目正式运行边界，依赖这些基建本身不是缺陷；需要补强的是基建契约显式化、启动诊断和可观测性。此前的运行时产物污染已处理，后续主要扣分项是**基建契约文档/诊断仍需显式化**、**admin 欠发育**、以及**双端/双 AI 运行时的边界稀释风险**。

---

## 二、研发架构（工程化）成熟度分析

按 CMM/DevOps 视角，从"能不能把质量固化成可重复流程"评估。

### 2.1 突出亮点：自研验证/审计 Gate 体系

这是本项目**最独特的工程资产**。根 `package.json` 定义了 **30+ 条 `verify:*` / `audit:*` / `check:*` / `smoke:*`** 脚本，覆盖：

- 边界校验：`verify:v2-boundaries`、`check-v2-boundaries.mjs`
- 完成度审计：`audit:v2-completion`（含 `--local` / `--quick`）
- 运行时闸门：`check:v2-runtime`、`run-v2-runtime-gates`
- 冒烟测试：`storage:smoke`、`upload:smoke`、`queue:smoke`、`queue:smoke:ai`（均有 object-storage 变体）
- 契约再生成：`generate:contracts` / `build:contracts`
- 本地一键运行时：`runtime:up`/`down`（docker-compose）+ `runtime:up:local`

配套 `docker-compose.runtime.yml` 一键拉起 PG16 + Redis7 + MinIO（含 healthcheck + bucket 初始化），`mock-ai-server` 支持无真实密钥跑通 AI 链路。**这套"可复现验证"能力已达到中大型团队水准，是研发成熟度的高分项。**

### 2.2 严重短板（拖累成熟度评级）

| 级别 | 问题 | 证据 | 影响 |
|---|---|---|---|
| **已处理** | **CI/CD 基线已建立** | `.github/workflows/ci.yml` 已接入，CI 统一调用 `npm run release:verify:no-runtime -- --skip-diff-check` | 本地 gate 已被钉进 PR 门禁；运行时 Docker smoke 仍保留为本地/部署前强验证。 |
| **已处理** | **Git 历史可追溯性已恢复** | 已从"仅 4 个提交"恢复为按主题拆分的连续提交，本轮按 CI、脚本、容器、任务队列、运行时验证、发布门禁、产品运行面分别提交 | 演进路径可追溯，后续可继续沿用小步提交 + review。 |
| **已处理** | **package.json 脚本引用漂移已加门禁** | `tools/verify-package-scripts.mjs` 已加入 `verify:package-scripts` 与 release gate；当前 7 个 package.json 校验通过 | 脚本清单从"靠记忆维护"变为可自动验证。 |
| **P1** | **测试覆盖薄且集中在近期** | 全仓仅 **29 个测试文件**（491 源文件），backend 单测多为本周新增（tasks/queue/images/videos），且用 hand-rolled mock（如 `createSelectDb`）而非集成测试 | 核心链路（AI 调用、媒体处理、端到端任务流）缺乏自动化回归保护。 |
| **P1** | **无统一代码规范强制** | 根目录无 eslint/prettier/husky/editorconfig；lint 仅存在于各 app 内部（web 有 eslint + commitlint，backend 仅 typecheck） | 跨包风格不统一，无 pre-commit 卡点，规范靠自觉。 |
| **已处理** | **backend 容器化交付物已补齐** | `apps/backend/Dockerfile` 已新增并优化为多阶段构建；`tools/verify-docker-runtime.mjs` 可验证 backend + worker 镜像与健康状态 | 后端业务中枢已有可复现容器交付路径。 |
| **P2** | **文档虽多但存在冗余/漂移** | `docs/`、`newdocs/`、`RELEASES/`、`VERSION_PLAN.md`、多份分析报告、`参考项目`/`参考图片` 目录并存 | 文档资产丰富（PRD/TRD/模块地图/审计报告齐全，是加分项），但组织分散、版本标注不一，新人难判断"哪份是当前真相"。 |

### 2.3 成熟度分维打分

| 子维度 | 评分 | 依据 |
|---|---|---|
| 代码组织 / 模块化 | ★★★★☆ | monorepo + workspaces + 清晰边界 + 共享包，优秀 |
| 契约 / 类型安全 | ★★★★★ | OpenAPI 单一真相源 + 全链路 TS，标杆级 |
| 本地可复现性 | ★★★★☆ | docker-compose + mock-ai + smoke 脚本，很强 |
| 自动化测试 | ★★☆☆☆ | 29 测试文件，覆盖薄，无集成/E2E 门禁 |
| CI/CD | ★★★☆☆ | GitHub Actions 基线已建立，release gate 已统一；Docker runtime gate 仍主要用于本地/部署前验证 |
| 版本控制纪律 | ★★★☆☆ | 已从 4 提交推进为按主题拆分的连续提交，媒体产物移出 Git；后续需持续保持小步提交和 PR review |
| 代码规范强制 | ★★☆☆☆ | 有工具无统一门禁 |
| 交付 / 部署 | ★★★★☆ | 前后端容器与 runtime compose 验证已具备；下一步是把部署文档、环境契约和监控告警补齐 |
| 文档 | ★★★★☆ | 数量与深度俱佳，但有漂移与冗余 |

---

## 三、关键改进建议（按优先级 / 投入产出比排序）

### P0（立即，低成本高收益）
1. **✅ 已完成：建立 CI 流水线**：`.github/workflows/ci.yml` 已接入统一 release gate，PR 门禁不再依赖人肉执行。
2. **✅ 已完成：清理版本库产物**：`.gitignore` 已覆盖 `apps/data/static/`，媒体产物已从 Git 跟踪中移除，改由对象存储/本地 runtime 目录承载。
3. **✅ 已启动：恢复正常提交纪律**：本轮已拆分为可解释的小步提交，后续保持 PR review + 变更分组提交。

### P1（近期）
4. **✅ 已完成：对齐脚本与工具**：`verify:package-scripts` 已建立，并纳入 release gate。
5. **✅ 已完成：补后端容器化**：`apps/backend/Dockerfile` 与 `runtime:verify` 已组成 backend/worker 可部署验证栈。
6. **✅ 已完成：统一发布门禁**：新增 `tools/run-release-gates.mjs`，把前端、后端、共享包、测试与 runtime smoke 串成单一 release gate。
7. **提升测试层级**：把 smoke 脚本进一步升格为可在 CI 跑的集成测试，覆盖任务队列端到端主链路（enqueue→claim→execute→webhook→terminal）。
8. **根目录统一 lint + pre-commit**：引入 husky + lint-staged，跨包强制格式与提交规范。

### P2（中期）
8. **收敛 AI 运行时边界**：明确 web(@ai-sdk/mastra) 与 backend(ai 模块) 各自职责，确立 AI 调用唯一真相源。
9. **补齐 admin 端**：将其从骨架推进到与 README 承诺匹配的治理能力。
10. **文档收敛**：合并 `docs`/`newdocs`，建立单一"当前真相"索引，修正版本标注漂移（Next 15 vs 16）。

---

## 四、总评

**小窗是一个"架构判断力强、工程制度尚欠"的高潜力项目。**

- **技术架构合理性 B+**：契约优先、前端零业务、异步任务的领域策略化、任务幂等与恢复设计，都体现了对生产级系统的真实理解。骨架正确，"能长大"的空间是真实存在的。扣分主要在运行时产物污染、基建契约显式化不足、双端/双 AI 边界稀释。

- **研发架构成熟度 B-**：拥有一套罕见且优秀的本地验证/审计 Gate 体系与可复现运行时；本轮已补齐 **CI/CD 基线、Git 提交纪律、脚本校验、后端容器与统一发布门禁**。剩余短板是测试覆盖深度、统一规范强制、部署契约文档和文档收敛。

一句话总结：**质量护栏已经装上门，下一步是把门禁做深、做稳、做长期。** 本项目的下一个跃迁不在于再写多少功能，而在于把 release gate 扩展到更真实的集成测试、部署契约与可观测性。

---

*报告基于对仓库的实际代码走读生成，结论可依据文中列出的文件路径与事实逐条复核。*
