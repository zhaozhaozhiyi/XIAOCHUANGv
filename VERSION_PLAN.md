# 小窗 XIAOCHUANG - 版本规划

**当前产品版本**: `0.22.0`
**上一版本**: `0.21.1`
**下一版本**: `0.23.0`
**最后更新**: 2026-07-04
**维护规则**: 每次规划、封版、改版本号时，先更新本文，再同步 GitHub 可见的 `RELEASES/`；如本地保留 `newdocs/`，再同步其中的版本事实源和版本索引。

## 1. 版本口径

小窗当前产品版本统一使用 `0.21.0` 及以上 SemVer 口径。

历史文档中的 `v1.x`、`v2.0`、`v2.2` 只作为历史阶段代号保留，不再作为当前排期口径。当前排期只看这条线：

| 版本 | 定位 | 状态 | 重点 |
|------|------|------|------|
| `0.21.0` | 核心创作闭环产品基线 | 已封版 | 前后端边界、后端真相源、任务/存储/AI Runtime、短剧/小说/画布/资产/设置主入口 |
| `0.21.1` | 稳定性与文档同步 patch | 已封版 | 创建入口口径、短剧长任务恢复、工作台视觉响应式、Settings Skill 校验 |
| `0.22.0` | 当前验收与产品逻辑收口版 | 当前版本 | 完整验收记录、首页/短剧产品逻辑、小说源稿到方案草稿、AI 调用顺畅度与错误可解释性 |
| `0.23.0` | 画布与资产闭环增强版 | 下一版本 | 画布产物真实入资产、上传闭环、生成历史、画布 Skill 对话代理最小闭环 |
| `0.24.0` | 画布 Skill 扩展与协作准备 | 后续规划 | 删除节点、建立/删除连线、运行编排体验增强 |
| `0.25.0` | 画布生成与资产回用深化 | 后续规划 | 触发生成、保存资产 Skill、资产反向回用、更多运行历史 |
| `1.0.0-beta` | 对外试用候选版 | 远期目标 | 主要链路稳定、文档与验收完整、已知风险可控 |

## 2. 当前版本 `0.22.0`

`0.22.0` 是在 `0.21.1` 稳定基线上的验收与产品逻辑收口版本。

本版本不是扩展新模块，而是把已经存在的首页、短剧、小说源稿、AI 调用和媒体任务链路整理成更能被真实用户理解、测试和交付的状态。

### 2.1 主要内容

| 方向 | 主要内容 | 价值 |
|------|----------|------|
| 验收候选 | 补齐 runtime full gate、对象存储 smoke、媒体队列 smoke、AI 队列 smoke、关键 E2E 记录 | 让 `0.21.x` 已跑通能力具备发布证据 |
| 首页入口 | 首页与短剧创建入口对齐 24 集默认、源稿导入、状态提示和创建失败保留输入 | 降低新建短剧后的认知跳跃 |
| 短剧详情 | 新增小说源稿导入/修复、源稿健康检查、目标集数/风格/节奏设置、方案草稿预览 | 让“小说/原稿 -> 短剧项目 -> 分集制作”形成合理产品闭环 |
| 方案草稿 | 将“生成改编方案”收口为“生成方案草稿”，明确该步骤是本地规则草稿，不消耗 AI 额度 | 避免用户误以为已调用 AI，产品预期更诚实 |
| 分集生成 | 从方案草稿批量生成分集初稿，进入单集工作台后再进行 AI 改写、分镜和媒体生产 | 把结构规划和 AI 生产分层，减少失败成本 |
| AI 顺畅度 | 文本/图片/视频/音频配置可见，封面生成做配置前置检查，工作台恢复运行中 AI 任务 | 让 AI 调用前置条件更清楚，刷新后状态不丢 |
| 错误可解释 | 新增统一 AI/媒体错误文案：配置缺失、参考图不可下载、敏感拦截、网络/轮询超时、任务缺少服务商 ID | 用户知道下一步该配置、重试、换图还是去任务中心 |

### 2.2 与上一版 `0.21.1` 的差异

| 模块 | `0.21.1` | `0.22.0` |
|------|----------|----------|
| 版本目标 | 稳定性与文档同步 patch | 验收与产品逻辑收口 minor |
| 首页 | 承接创作入口，替代独立 `/create` 聚合页 | 进一步对齐短剧创建逻辑、默认集数、错误保留和源稿预期 |
| 短剧详情 | 以项目、分集、角色、场景、制作入口为主 | 增加小说源稿、方案草稿、目标设定、分集生成前置规划 |
| 短剧工作台 | 长任务恢复、状态可读、E2E 覆盖 | 扩展 AI/媒体错误解释与超时提示，降低“调用失败但不知道为什么”的挫败感 |
| AI Runtime | 统一 Skill 驱动、任务落库与恢复 | 明确哪些步骤真正调用 AI，补充配置前置检查和失败原因翻译 |
| 验收记录 | quick gate 和关键工作台复核 | full gate、对象存储、队列、AI 队列、关键 E2E 形成发布证据 |
| 画布 | 占位入口收口，真实资产闭环后移 | 继续保持后移，明确进入 `0.23.0` 专项 |

### 2.3 已知风险

| 风险 | 等级 | 说明 | 处理版本 |
|------|------|------|----------|
| 外部服务商下载本地媒体 URL 失败 | Medium | 本地 `127.0.0.1/static` 在真实云端模型侧不可访问时，图生视频参考图会失败 | `0.22.x` 先提示清楚，`0.23.0` 结合对象存储闭环强化 |
| 方案草稿是规则生成，不是 AI 改编 | Low | 已通过文案收口为“方案草稿”，后续可评估接入文本 AI 生成正式改编方案 | `0.23+` 视优先级规划 |
| 画布资产闭环仍未完全实证 | Medium | 画布上传、保存到资产、资产回源仍按既定计划进入下一版 | `0.23.0` |
| 版本文档多入口历史包袱 | Low | 已新增根 `VERSION_PLAN.md` 作为稳定入口 | `0.22.0` 已处理 |

## 3. 下一版本 `0.23.0`

`0.23.0` 聚焦画布可信闭环和 Skill 驱动 AI 对话代理最小闭环。

### 3.1 目标

画布不再只是一块编辑区域，而要成为可沉淀、可追溯、可回用的生产空间：

- 画布结果能保存到资产库。
- 资产能打开来源画布。
- 上传能真实进入画布，并可选择是否入资产。
- 节点生成历史可见、可切换、可下载、可保存资产。
- 对话栏开放 3 个最小 Skill：`canvas.create_nodes`、`canvas.update_node`、`canvas.add_to_context`。

### 3.2 必做范围

| 优先级 | 范围 | 说明 |
|--------|------|------|
| P0 | 资产闭环 | 画布生成结果真实写入资产，资产库可搜索/预览/下载/打开来源 |
| P1 | 上传闭环 | 画布上传真实可用，支持图片/视频/音频进入画布，可选保存资产 |
| P2 | 生成历史 | 节点保留生成历史，支持切换当前结果、下载和保存资产 |
| P3 | 最小快速生成 | 空白提示词或单节点触发快速生成，结果插入为可见节点 |
| P4 | Skill 对话代理 | 对话栏拥有画布上下文，支持创建节点、修改节点、加入上下文 |
| P5 | 镜头主线体验 | 镜头节点顺序排列、角色/场景端口连线、右侧镜头控制台、顶部进度 |

### 3.3 不做范围

- 不做多人实时协作。
- 不做复杂分支版本合并。
- 不做画布直接覆盖短剧源数据。
- 不做完整时间线剪辑工具。
- 不做完整 8 个 Skill，全量 Skill 分到 `0.24.0` / `0.25.0`。
- 不做文件解析式“导入剧本生成镜头”，`0.23.0` 先支持在对话栏粘贴文本描述创意。

## 4. 发布纪律

每个版本必须维护这些内容：

1. 当前版本号、上一版本、下一版本。
2. 版本定位：为什么做这个版本。
3. 必做范围：做到什么算完成。
4. 不做范围：明确避免范围漂移。
5. 验收门禁：至少包含构建、类型、lint 或 E2E/烟测证据。
6. 已知风险：哪些问题接受带着走，进入哪个版本处理。
7. 更新日志：记录日期和变化。

## 5. 文档入口

以后找版本规划优先看本文：

- 根入口：[VERSION_PLAN.md](./VERSION_PLAN.md)
- 发布说明索引：[RELEASES/README.md](./RELEASES/README.md)
- 当前版说明：[RELEASES/0.22.0.md](./RELEASES/0.22.0.md)

本地协作文档如存在于 `newdocs/`，需要与本文保持同一版本口径；但 `newdocs/` 当前被 `.gitignore` 排除，不作为 GitHub 公开入口。
