# 0.23 待办评估清单

整理日期：`2026-07-06`
评估日期：`2026-07-06`
状态同步：`2026-07-09` 已确认 `0.23.2` 封版，本文中 `0.23.2` 事项更新为已完成或已封版归档；`0.23.3` 暂缓独立排期，后续范围按短剧项目中枢需要并入 `0.24.0` 或后续版本。

来源范围：

- `docs/VERSION_PLAN.md`
- `docs/0.23/0.23版本计划.md`
- `docs/0.23/短剧AI-first改编流程重构.md`
- 已归档的 0.22 架构复审报告
- 根目录 `README.md`、`RELEASES/`

本文汇总“后续需要评估或推进”的事项，并标记当前代码与文档中的进展情况。

进展状态说明：

- `已基本落地`：已有主要后端、前端或数据链路，下一步以验收、补测试和体验收口为主。
- `部分落地`：已有关键雏形，但与 0.23 目标仍有明显缺口。
- `方案完成`：产品方案已落档，但研发实现尚未展开或未形成最小闭环。
- `未开始`：当前代码中未发现对应实现。
- `未完成`：已有相关历史实现或旧实现，但尚未达到当前事项要求。
- `后移保留`：维持后续版本承接，不建议塞回 0.23。
- `待决策`：需要产品或技术评审后决定版本归属。

## 1. 文档现状判断

### 1.1 已归档，不再作为当前入口

- `docs/_archive/baseline-docs-before-0.23-folder-2026-07-06/PRD.md`
- `docs/_archive/baseline-docs-before-0.23-folder-2026-07-06/TRD.md`
- `docs/_archive/baseline-docs-before-0.23-folder-2026-07-06/VERSION.md`
- `docs/_archive/baseline-docs-before-0.23-folder-2026-07-06/modules/`
- `docs/_archive/baseline-docs-before-0.23-folder-2026-07-06/versions/`

判断：这些文件多为 `0.21` 到 `0.22` 基线内容，已不适合作为当前需求入口。

### 1.2 当前 0.23 事实入口

- `docs/0.23/README.md`
- `docs/0.23/0.23版本计划.md`
- `docs/0.23/短剧AI-first改编流程重构.md`

判断：后续评审 0.23 需求时，优先看上述文件。

### 1.3 可继续保留在根目录

- `README.md`：对外项目介绍，需随实际技术栈和版本口径维护。
- `docs/VERSION_PLAN.md`：跨版本总入口。
- `RELEASES/`：GitHub 可见发布说明。

判断：这些不是详细需求文档，但仍是对外或跨版本入口。

## 2. 0.23.x 必评估事项

| 编号 | 目标版本 | 优先级 | 事项 | 进展状态 | 当前判断 | 下一步动作 |
|---|---|---|---|---|---|---|
| TODO-0.23-08 | 0.23.1 | P0 | 短剧 AI-first 改编流程：移除“方案草稿”主流程对象，改为源稿理解、策略、蓝图、试播、生产 | 方案完成 | 文档方案已完整落档；当前代码仍大量使用 `adaptationPlan/方案草稿`，数据库也没有 `source_analysis/adaptation_brief/episode.blueprint_payload` 等字段 | 进入 0.23.1 主线；先做最小切片，不一次性全量替换 |
| TODO-0.23-09 | 0.23.1 | P1 | 新增短剧项目子页：`source`、`strategy`、`episodes`、`assets`、`settings` | 未开始 | 当前只有 `/drama/[id]/page.tsx` 和单集工作台路由，未发现这些项目子页 | 0.23.1 优先 `source + episodes + assets`；`strategy/settings` 可先内嵌或后移 |
| TODO-0.23-10 | 0.23.1 | P2 | 单集工作台公共资产引用：公共主档、单集覆写、镜头私有产物三层模型 | 部分落地 | 已有 `characters`、`episode_characters`、`scenes`、`episode_scenes` 基础关系，工作台会读取项目角色/场景；但没有 `override_payload/link_source/scenes.scope/master_scene_id` 等覆写与继承字段 | 进入 0.23.1 前置设计；先明确迁移规则，再动数据库和工作台 |
| TODO-0.23-01 | 0.23.2 | P0 | 画布资产闭环：画布生成结果、历史结果真实进入资产库，并可从资产库回到来源画布 | 已完成 | `0.23.2` 已完成画布来源类型收敛、历史/上传入资产、资产库展示与回源 E2E | 已随 `0.23.2` 封版归档；后续仅评估 DB 级 `source_type` 约束 |
| TODO-0.23-02 | 0.23.2 | P1 | 画布上传闭环：图片、视频、音频上传进入画布，可选保存到资产 | 已完成 | `0.23.2` 已完成图片 30MB、视频 200MB、音频 100MB 分档限制、失败提示和上传入资产验收 | 已随 `0.23.2` 封版归档 |
| TODO-0.23-03 | 0.23.2 | P2 | 画布节点生成历史：结果追加、切换、下载、保存资产，最多保留 20 条 | 已完成 | `0.23.2` 已补事务/行锁、并发写保护、`canvas-node-result.service.test.ts` 和历史面板 E2E | 已随 `0.23.2` 封版归档 |
| TODO-0.23-04 | 0.23.2 | P3 | 最小快速生成：空白提示词或单节点生成，并插入可见节点 | 已完成 | `0.23.2` 已确认快速生成默认进入节点历史、不自动进入资产，并完成空白/单节点入口 E2E | 已随 `0.23.2` 封版归档 |
| TODO-0.23-05 | 0.23.3 | P0 | 画布对话 Skill 最小闭环：`create_nodes`、`update_node`、`add_to_context` | 部分落地 | 已有 `CanvasChatAgentService`、`CanvasSkillService`、`ChatDock` 和确认计划；当前是规则触发的最小代理，`add_to_context` 还不是持久上下文，AI 推理能力较弱 | 后移到 0.23.3；先按“规则版最小闭环”验收，再评估是否接入真实 LLM 规划 |
| TODO-0.23-06 | 0.23.3 | P1 | 角色/场景端口连线到镜头节点 | 未完成 | `character/scene` 节点有输出端口，但 `storyboard` 节点没有 `character/scene` 输入端口，且前端仍保留“关联到分镜/设为分镜背景”的旧关联模式 | 后移到 0.23.3；修改共享节点定义、连线校验和 UI，移除或降级旧关联模式 |
| TODO-0.23-07 | 0.23.3 | P2 | 合成节点吸收导出能力，导出产物按 `canvas_export` 入资产 | 部分落地 | 后端 `concat/export` 执行结果会按 `canvas_export` 进入节点结果；但共享节点定义里 `concat` 与 `export` 仍是两个节点，尚未完成“合成节点吸收导出能力”的产品收口 | 后移到 0.23.3；决定是否删除独立 `export` 节点或仅在 UI 隐藏，并补导出入资产验收 |

## 3. 0.24 / 0.25 后续承接事项

| 编号 | 版本建议 | 事项 | 进展状态 | 当前判断 | 下一步动作 |
|---|---|---|---|---|---|
| TODO-NEXT-01 | 0.24 | `canvas.delete_nodes` | 后移保留 | 画布 UI 已可删除节点，但对话 Skill 未开放删除节点 | 维持 0.24，避免扩大 0.23 Agent 范围 |
| TODO-NEXT-02 | 0.24 | `canvas.create_edge` | 后移保留 | 画布手动连线已存在，Skill 未开放建连线 | 维持 0.24，先等 P5 端口模型收口 |
| TODO-NEXT-03 | 0.24 | 对话删除节点、对话建立连线 | 后移保留 | 当前 Chat Agent 明确拒绝“删除/连线”等动作 | 维持 0.24，补权限确认和操作预览后再做 |
| TODO-NEXT-04 | 0.25 | `canvas.generate` | 后移保留 | 画布已有手动业务动作与 run 执行链路，但对话触发生成被明确拦截 | 维持 0.25，等生成历史和资产闭环稳定后开放 |
| TODO-NEXT-05 | 0.25 | `canvas.delete_edge` | 后移保留 | 手动边编辑能力存在，Skill 未开放删边 | 维持 0.25 |
| TODO-NEXT-06 | 0.25 | `canvas.save_to_assets` | 后移保留 | 手动保存节点结果到资产已基本落地；对话 Skill 未开放保存资产 | 维持 0.25；0.23 只验收手动保存 |
| TODO-NEXT-07 | 0.25 | 对话触发生成、删除连线、保存资产 | 后移保留 | 属于高风险动作，当前 Agent 已做范围拦截 | 维持 0.25 |
| TODO-NEXT-08 | 后续增强 | 对话上下文跨会话保持 | 未开始 | 当前 chat store 是前端会话态，`add_to_context` 仅返回本次操作结果 | 后续增强，不进入 0.23 |
| TODO-NEXT-09 | 后续增强 | 对话回溯和撤销 | 未开始 | 画布有本地历史 store，但对话级回溯/撤销未设计 | 后续增强 |
| TODO-NEXT-10 | 后续增强 | 对话引导式创作流程 | 部分落地 | 旧 `usePipelineOrchestrator` 有引导式生成雏形，但与 0.23 Chat Skill 主链路不是同一套 | 后续统一到 Agent 设计里，当前不扩大 |
| TODO-NEXT-11 | 后续增强 | 画布运行历史列表、节点输出版本预览 | 部分落地 | 已有 `canvas_versions`、run status、节点结果历史；缺少统一运行历史产品页和更完整的版本预览 | 后续增强；0.23 只保节点生成历史 |

## 4. 工程债待评估

来自已归档报告：

- `docs/_archive/0.22.0-architecture-review-reports/技术架构合理性与研发成熟度分析报告.md`
- `docs/_archive/0.22.0-architecture-review-reports/架构与交互设计分析报告.md`

| 编号 | 优先级 | 事项 | 进展状态 | 当前判断 | 下一步动作 |
|---|---|---|---|---|---|
| DEBT-01 | P1 | 提升测试层级，把 smoke 脚本逐步升格为可在 CI 跑的集成测试 | 部分落地 | 已有后端 Vitest、画布后端单测、Web Playwright E2E 和 smoke 脚本；`0.23.2` 已补 Canvas 专项单测与 E2E，`0.23.3` 画布 Agent 仍需专项测试 | 0.23.3 继续补 chat skill、端口连线和合成导出相关测试 |
| DEBT-02 | P1 | 根目录统一 lint / format / pre-commit 规则 | 未完成 | 根目录无统一 lint/format/pre-commit 配置；仅 app 级 eslint 和部分依赖 | 后续工程治理项，不阻塞 0.23.1 短剧主流程闭环 |
| DEBT-03 | P1 | 拆分 `use-workbench.ts`，提取 `taskTrackingStore` 和单一剧集轮询器 | 部分落地 | 已有单一剧集轮询器雏形和 entity task tracking，但 `use-workbench.ts` 仍是大型 store | 进入 0.23.1 短剧重构前置工程债，至少拆 task tracking 与导航计算 |
| DEBT-04 | P2 | 全局 `running` 单槽位改为 per-task-type 计数 | 部分落地 | 已有 `pendingCharImages/pendingSceneImages/pendingVideos/entityTasks` 等分项状态；但顶层 `running/runningType` 仍是单槽位 | 后续逐步改为 per-task-type 计数，避免批量任务互相阻塞 |
| DEBT-05 | P2 | 真乐观更新：先写本地 + pending，失败回滚 | 未完成 | 当前多处是请求后刷新或本地 pending 标记，未形成通用 pending/rollback 机制；`0.23.2` 已先完成画布上传、节点历史保存的可信闭环验收 | 通用 pending/rollback 机制后续统一设计 |
| DEBT-06 | P2 | 轮询超时改为持久 banner 或任务中心提示 | 部分落地 | 工作台已有实体任务恢复、失败提示和部分超时处理；`0.23.2` 已补画布上传/生成相关失败提示，但仍不是统一持久任务中心 | 统一任务中心提示后续评估 |
| DEBT-07 | P2 | `StorageService` 是否切到 AWS SDK，并收敛 s3 模式本地缓存策略 | 待决策 | 当前是自实现 S3 签名 + 本地缓存，未切 AWS SDK；是否更换取决于部署环境和维护成本 | 技术评审后再决定，不作为 0.23.x 功能阻塞 |
| DEBT-08 | P2 | 合并两套前端 API client | 未完成 | `apps/web/src/lib/api.ts` 与 `apps/web/src/lib/canvas/api/client.ts` 仍并存 | 后续收敛为统一 fetch/envelope/缓存策略 |
| DEBT-09 | P2 | env 收敛：`S3_REGION`、`DATABASE_URL` 默认值、已知键校验 | 部分落地 | 已有 zod env schema，`DATABASE_URL/REDIS_URL` 必填，S3 配置有校验；`S3_REGION` 未强制，schema 仍 `passthrough` 允许未知键 | 后续补已知键诊断和 S3_REGION 策略 |
| DEBT-10 | P2 | 领域 handler 多写操作包 DB transaction | 部分落地 | 画布 save/restore 已使用 transaction；任务 domain handlers、drama split/create 等多写操作仍多为分散写入 | 按高风险链路逐步补 transaction，不阻塞 0.23.1 P0/P1 |
| DEBT-11 | P2 | 画布与短剧主链路的集成边界 | 已定边界 | `0.23.2` 已按“只做资产和来源追溯，不做画布覆盖短剧数据”封版 | 后续如需加深集成，必须重新评审边界 |
| DEBT-12 | P2 | admin 端治理能力是否进入近期版本 | 部分落地 | 已有 `apps/admin` 和后端 admin controllers；是否近期增强仍是版本策略问题 | 待版本评审，不进入 0.23 主线 |
| DEBT-13 | P2 | 前后端 AI Runtime 边界：AI 调用真相源是否唯一 | 部分落地 | 短剧工作台已统一走 `/ai/runs`；画布仍有 `business-action/run` 直接调图片/视频/音频服务的执行链路，`0.23.2` 已优先保证画布执行链路可追溯 | 后续评估是否统一到 AI Runtime |
| DEBT-14 | P2 | 外部基建契约显式化：部署契约、healthcheck、启动前诊断、错误提示 | 部分落地 | 已有 env 校验和 `/health` 数据库检查；缺少更完整的对象存储、Redis、AI provider 启动前诊断 | 后续补 healthcheck 维度和启动诊断报告 |
| DEBT-15 | P3 | 删除 `.stage-subnav` 死 UI、修重复 `char-count` span | 未完成 | `stage-subnav` 仍在单集工作台页面和 CSS 中，`char-count` 在多个面板重复使用 | 进入 UI 清理项；可随单集工作台重构处理 |
| DEBT-16 | P3 | 统一 `nextNav / goContinue / isDone / pipelineProgress` 为单一来源 | 未完成 | `nextNav` 在 `use-workbench.ts`，`goContinue/isDone` 在页面，`pipelineProgress` 在 store，逻辑仍分散 | 随 `use-workbench.ts` 拆分一起处理 |
| DEBT-17 | P3 | TTS `refreshPresentation` 无法重建错误展示，需补注释或结构化来源 | 部分落地 | `refreshPresentation` 已存在，可从 storyboard 音频回填完成态；但错误来源和不可重建原因没有结构化说明 | 后续补注释、错误来源字段或恢复失败记录 |

## 5. 已处理或已归档事项

| 事项 | 处理结果 |
|---|---|
| `docs/PRD.md` 和 `docs/TRD.md` 是否过时 | 已归档到 `baseline-docs-before-0.23-folder-2026-07-06` |
| `newdocs` 与 `docs` 双入口 | 已收敛为 `docs/0.23` 当前入口，旧资料归档 |
| 低保真原型 HTML | 已删除，不再作为交付物 |
| 0.22 审计和真人测试报告 | 已归档到 `0.22.0-audit-and-test-reports` |
| 0.22 架构复审报告 | 已归档到 `0.22.0-architecture-review-reports` |
| 根 README 技术栈版本漂移 | 已确认 Next 实际为 `^15.5.15`，根 README 已修正为 Next.js 15 |

## 6. 暂不纳入当前待办的事项

- 多人实时协作
- 复杂分支版本合并
- 完整时间线剪辑工具
- 画布直接覆盖短剧源数据
- 画布全量替代短剧工作台
- 独立低保真原型页
- 独立“方案草稿”页
- 独立角色圣经页
- 独立场景圣经页
