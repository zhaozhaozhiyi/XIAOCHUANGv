# 小窗 XIAOCHUANG — 架构与交互设计分析报告（第二轮复审）

> 收口基线：产品版本 `0.22.0`｜代码收口 commit `ed34d09`（P0/P1-F/P1-G 已提交；文档提交另计）
> 评审范围：本轮针对第一轮报告的 P0/P1 建议所做的重构 + 新引入代码
> 评审方法：`git diff` 全量走读 + 新增文件逐行阅读 + 交叉验证 bug 修复点
> 本轮结论：**P0 三项全部命中；P1 已继续推进到 release gate、脚本校验、后端容器、runtime 验证、image upsert、listTasks SQL 下推与 worker 可运维性。剩余主要债务集中在前端 god-object 拆分、registry 未知领域语义、TTS 错误分类与事务边界。**

---

## 0. 本轮变更总览

| 维度 | 变化 |
|---|---|
| 后端 tasks 模块 | **大重构**：`tasks.service.ts` -731 行、`task-execution.service.ts` -234 行，抽出 `domain-handlers/`（5 个 handler + base + utils）+ `task-domain.registry.ts` |
| 后端 images/videos.tasks | 补全 `episodeId` 解析；`image_generations` task 同步已加活跃唯一约束 + `onConflictDoNothing`，避免并发 double-insert |
| 后端 tasks API | `listTasks` 已改为 SQL 过滤/排序/分页，避免全量拉内存再筛 |
| 后端 queue/worker | BullMQ 默认 `attempts/backoff/removeOn*` 已显式化，worker processor 增加结构化日志，recover worker 支持 readiness file |
| 后端 videos.service | 修 webhook 幂等：`handleVideoComplete` / `handleViduWebhook` 前置终态检查 |
| 后端 storage | 新增 `STORAGE_OBJECT_ACL` / `x-cos-acl` 头，SigV4 签名动态纳入该头 |
| 后端 env | `STORAGE_S3_FORCE_PATH_STYLE` 改用 `booleanEnv` 预处理器（替代 `z.coerce.boolean()`） |
| 前端 use-workbench | 任务恢复态与 pending 状态显著增强；当前文件约 2053 行，说明体验补强已有效，但拆 store 的必要性更高 |
| 前端 production/export panel | 统一 `isPending = localPending \|\| isActiveWorkbenchTask(task)`，全卡片一致的"生成中/失败/重试"状态 + 错误条 + 重试按钮 |

截至收口，后端核心债（领域分发复制、attemptCount 双计数、webhook 幂等、image upsert 竞态、listTasks 全量分页、worker 基础可运维性）已连续处理；发布层也补上 CI、脚本校验、后端 Dockerfile、Docker runtime smoke 与统一 release gate。前端体验层明显前进，但 `use-workbench.ts` 继续膨胀，是下一轮最值得优先拆的点。

---

## 一、第一轮 P0/P1 建议的修复验证

### ✅ P0-1：`toast.success('生成中...')` 语义错误 — 已修复

`use-workbench.ts` 全部 6 类生成动作（角色图、场景图、配音、镜头图、镜头视频、合成）+ 合并，统一改为三段式反馈：

- 提交时 `toast.info('...已提交')`（如 `:333 角色图片生成已提交`、`:487 镜头视频生成已提交`、`:555 合成任务已提交`）
- 轮询完成 `toast.success('...生成完成')`
- 轮询超时 `toast.error('...仍未完成', { description: '任务可能还在后台排队或处理。当前卡片会保留状态，可稍后查看失败原因并重试。' })`

语义正确，且超时描述给了用户可操作的下一步。**这是本轮最直接的体验提升。**

### ✅ P0-2：领域分发 5×5 复制 — 已修复，实现质量高

引入了教科书式的策略 + 注册表：

```
TaskDomainHandler (interface)          # retry/cancel/refreshPresentation/markCanceled/markFailed/execute
  └─ BaseTaskDomainHandler (abstract)  # syncTaskUpdate / cancelTaskRecord / isTaskCanceled / now()
      ├─ ImageGenerationTaskHandler
      ├─ VideoGenerationTaskHandler
      ├─ StoryboardTtsTaskHandler
      ├─ StoryboardComposeTaskHandler
      └─ VideoMergeTaskHandler
TaskDomainRegistry                     # Map<domainTable, handler>，retry/cancel 缺 handler 抛 ConflictException
task-domain-utils.ts                   # inferErrorKind/mapGenerationStatus/parseJsonValue/trimText/sanitizePayload 单一来源
```

亮点：
- `TasksService.retryTask/cancelTask/refreshTaskPresentation` 现在是 3-5 行的薄委托（`tasks.service.ts:97-113`）。
- `TaskExecutionService.markTaskFailed/markTaskCanceled` 同样委托（`task-execution.service.ts`，~200 行 switch 被替换为 2 行调用）。
- `inferErrorKind` / `mapGenerationStatus` 等工具函数从 3 份拷贝收敛为 1 份（`task-domain-utils.ts`）。
- 注册表构造在 DI 构造函数里完成，`requireHandler` 对 retry/cancel 抛 `ConflictException('当前任务暂不支持重试')`——扩展新领域只需注册一个 handler，符合预期。

**新增 `execute` 方法也收进了 handler**，比第一轮建议更进一步：`TaskExecutionService.executeTask` 末尾 `return this.taskDomainRegistry.execute(task)`，把领域执行也归口了。

### ✅ P0-3：`attemptCount` 双重计数 — 已修复

旧逻辑：`retryX` 里 `attemptCount: (task.attemptCount ?? 0) + 1`，`claimQueuedTask` 再 +1 → 一次手动重试消耗 2 次配额。

新逻辑：所有 handler 的 `retry()` 方法**只重置状态字段，不再碰 `attemptCount`**（对照 `image-generation-task.handler.ts:retry`，syncTaskUpdate 里没有 attemptCount）。`attemptCount` 仅在 `claimQueuedTask` 的 `UPDATE ... RETURNING` 里自增。`MAX_RETRY_ATTEMPTS=7` 现在就是 7 次真实 claim。✅

### ✅ P1-5：Webhook 幂等缺口 — 已修复

`videos.service.ts` 新增 `isTerminalVideoGenerationStatus()`（`completed/failed/canceled/cancelled`）：

- `handleVideoComplete`（`:535`）：`if (!record || isTerminalVideoGenerationStatus(record.status)) return`——已终态直接返回，不再被重试 webhook 复活成 completed。
- `handleViduWebhook`（`:587`）：进入处理前 `if (isTerminalVideoGenerationStatus(record.status)) return { message: 'Task already X' }`——provider 重推 webhook 时幂等返回。

### ⚠️ P1 后续状态

- ✅ P1-5 `syncTaskForImageGeneration` upsert 竞态：已处理。
- ✅ P1-6 `listTasks` 全量内存分页：已处理。
- ✅ P1-8 worker 可运维性基础项：已处理到结构化日志、readiness、attempts/backoff；后续可继续补指标/告警。
- 🔴 P1-7 前端 god-object：未处理，且因任务恢复增强继续变大。
- 🟠 P1-4 / P1-9 / 事务边界：仍需收敛。

---

## 二、本轮新引入的交互改进（第一轮 P2 范畴，主动做了）

### 实体卡片状态统一：本地 pending + 服务端 task 双源合一

`production-panel.tsx` 全部实体卡片（角色图、场景图、配音、首/尾帧、镜头视频、合成）现在统一用：

```ts
const isPending = wb.pendingCharImages.has(c.id) || isActiveWorkbenchTask(task)
```

这解决了一个第一轮没单列但很烦的体验问题：**刷新前本地 `pendingX` 是乐观标记，刷新后 `entityTasks` 是服务端真实态，两者以前各自渲染、状态会"闪一下"。** 现在两者合一，卡片状态在"生成中 / 生成失败 / 已生成 / 待生成 / 重试"之间一致切换，按钮在 pending 时 `disabled`，失败时显示"重试"。配套补了 `.entity-task-notice.is-error` 错误条 + `.asset-cover-badge.is-error` + `.dot.error` 徽标（`episode-panels.css`）。

`export-panel.tsx` 同步：合并任务现在显示"合并中，旧成片会保留到新结果完成"、失败时显示错误条 + 重试按钮、按钮在合并中禁用并转 `Loader2`。

这是本轮**最有价值的交互改进**——把"任务恢复态"和"乐观态"在视觉上统一了。

### `recoveredEntityPollSerial` 串行守卫

`use-workbench.ts:284-289`：恢复态轮询用 `++recoveredEntityPollSerial` 生成序号，轮询循环里 `if (pollSerial !== recoveredEntityPollSerial) break`。快速切换剧集时旧轮询会自行退出。**部分缓解了第一轮 P2-7 的闭包过期问题**——但只覆盖了恢复态轮询，几十个 `genXxx` 主轮询仍无守卫（见下）。

---

## 三、仍未处理的问题（按优先级，更新版）

### ✅ P1：`syncTaskForImageGeneration` upsert 竞态已修

`images.tasks.ts` 当前已：

- 查找 existing 时过滤 `isNull(tasks.deletedAt)`；
- 插入时使用 `.onConflictDoNothing({ target: [tasks.domainTable, tasks.domainId], where: sql\`${tasks.deletedAt} IS NULL\` })`；
- 配套迁移 `0008_tasks_domain_active_unique`，确保同一领域实体同一时刻只有一个活跃 task；
- 冲突后重新读取 active task，避免并发 sync double-insert。

这条可以从 P1 风险中移除。

### ✅ P1：`listTasks` 全量内存分页已修

`tasks.controller.ts` 当前已把 `type/source_type/status/drama_id/episode_id/q` 条件下推到 SQL，使用 `count()` + `orderBy()` + `limit()` + `offset()` 返回分页结果。`q` 使用 `ilike(tasks.title, ...)`。这条可以从 P1 风险中移除。

### 🔴 P1：前端 god-object 未拆，且继续增大（当前约 2053 行）

本轮在 `use-workbench.ts` 里**继续往单 store 堆逻辑**（任务恢复、entityTasks、pending 映射、重试入口、轮询状态都在同一个 store 里），没有按第一轮建议拆成 `domainStore / navStore / taskTrackingStore / editStore / uiStore`。后果：

- **过度订阅未缓解**：绝大多数 `useWorkbench()` 仍无 selector，本轮新增的 `isActiveWorkbenchTask(task)` 在每张卡片 `render` 里调用，叠加流式 `set({ localScript })` 仍会触发全面板重渲染。
- **N 条独立轮询未收敛**：每个 `genXxx` 仍各自起 3-5s × 60-120 次轮询，每次全量 `episodeAPI.storyboards(episode.id)`。本轮没引入共享 poller。
- **闭包守卫只覆盖恢复态**：`recoveredEntityPollSerial` 只管恢复轮询，主轮询仍可能在快速切剧集时写穿 store。

**建议**：本轮的 toast/状态统一逻辑（+327 行）正好是拆分的契机——把 `pendingX / entityTasks / genXxx 轮询` 整体抽成 `taskTrackingStore` + `useEpisodePolling` 单轮询器，既拆了 god-object，又顺手收敛了 N 条轮询。

### ✅/⚠️ P1：worker 基础可运维性已补，后续补指标/告警

已完成：

- `task-queue.shared.ts` 显式定义 `attempts/backoff/removeOnComplete/removeOnFail`；
- `task-worker.processor.ts` 输出结构化 job processing/error 日志，包含 job id/name/data/attempts；
- `worker.ts --ready-file` 支持 readiness 文件，runtime 验证会检查 worker ready/health；
- Docker runtime smoke 已覆盖 backend health + worker readiness。

后续仍建议补：Prometheus/OTel 指标、死信/重试告警、队列积压阈值、provider 分类型失败统计。

### 🟡 P2：全局 `running` 单槽位、真乐观更新、持久 banner、SigV4/本地缓存、两套 API client、env 收敛、画布联动

均未处理（与第一轮一致，不重复展开）。

### 🟡 P2（新发现）：`VideoMergeTaskHandler.retry` 的 payload 推导有重复求值

```ts
const videos = Array.isArray(parseJsonValue(merge.scenes))
  ? parseJsonValue(merge.scenes)                    // 第二次调用
  : parseJsonValue(task.payloadJson)?.videos        // parseJsonValue 返回 unknown，.videos 不安全
```

`parseJsonValue(merge.scenes)` 调了两次；且 `parseJsonValue` 返回 `unknown | null`，直接 `.videos` 是不安全的类型访问。**建议**：先 `const scenes = parseJsonValue(merge.scenes)` 再判断，payload 用类型守卫。

---

## 四、重构本身引入的新瑕疵（需收敛）

### 4.1 Registry 对"未知领域"的行为不一致

`task-domain.registry.ts`：

- `retry` / `cancel` 用 `requireHandler` → 抛 `ConflictException`（明确报错）。
- `markCanceled` / `markFailed` / `execute` 用 `getHandler(task)?.xxx ?? false/'unknown'` → **静默 no-op**。

如果将来出现一个 `domainTable` 不在注册表里的旧任务（数据迁移、历史脏数据），`markFailed` 会静默返回 `false`，任务**既没被标记失败也没被标记取消**，卡在 running 直到锁过期。建议：`markFailed/markCanceled/execute` 也走 `requireHandler`，或在 `?? false` 分支补一条 `logger.warn` + 显式置 failed。

### 4.2 `StoryboardTtsTaskHandler` 仍硬编码 `errorKind: 'provider'`

`tts handler` 的 `markFailed` 直接写 `errorKind: 'provider'`，不调用 `inferErrorKind(message)`。一个网络超时导致的 TTS 失败会被错分类为 provider 错误。这是第一轮已指出的漂移点，**重构时原样搬过来了，没顺手修**。`task-domain-utils.ts` 已经导出了 `inferErrorKind`，TTS handler 完全可以用 `inferErrorKind(message)`。建议本轮内顺手修掉。

### 4.3 Handler 多写操作仍无事务

每个 handler 的 `retry` / `cancel` / `markFailed` 都做"更新领域行 + syncTaskUpdate"两次写入，**仍无 DB 事务包裹**。第二次写失败时，领域行已重置为 `pending` 但 task 行仍是 `failed`，状态分裂。第一轮已指出，重构未引入事务。建议在 `BaseTaskDomainHandler` 提供一个 `runInTransaction(cb)` 模板方法。

### 4.4 `execute` 返回 `'unknown'` 与 worker 调度语义

`TaskExecutionService.executeTask` 末尾 `return this.taskDomainRegistry.execute(task)`。若返回 `'unknown'`（handler 缺失），调用方（worker）拿到非终态字符串，任务既不失败也不完成。与 4.1 同源，建议统一。

### 4.5 `StoryboardTtsTaskHandler.refreshPresentation` 不回写错误字段

TTS 没有独立领域表，错误态只存在于 task 行本身。`refreshPresentation` 只按 `ttsAudioUrl` 有无判 completed/原状态，不回写 `errorKind/errorMessage`。如果 task 行错误字段被清空，refresh 无法重建错误展示。这是 TTS 领域的结构性限制（无 errorMsg 来源），可接受，但建议在 handler 里加注释说明，避免后续维护者误以为是 bug。

---

## 五、更新后的优先级行动表

> 标注：✅已修复 / ⚠️部分修复 / 🆕新发现 / 🔴本轮未处理

| # | 优先级 | 状态 | 行动 | 触点 |
|---|---|---|---|---|
| 1 | P0 | ✅ | toast 语义三段式 | `use-workbench.ts` gen* |
| 2 | P0 | ✅ | 领域 handler 注册表 | `domain-handlers/*` + `task-domain.registry.ts` |
| 3 | P0 | ✅ | attemptCount 单点计数 | handler `retry()` / `claimQueuedTask` |
| 4 | P1 | ⚠️ | TTS handler 用 `inferErrorKind(message)` 替代硬编码 `'provider'` | `storyboard-tts-task.handler.ts:markFailed` |
| 5 | P1 | ✅ | `syncTaskForImageGeneration` 加唯一约束 + `onConflictDoNothing` + 统一 deletedAt 过滤 | `images.tasks.ts` + `0008_tasks_domain_active_unique` |
| 6 | P1 | ✅ | `listTasks` filter/sort/pagination 下推 SQL | `tasks.controller.ts:listTasks` |
| 7 | P1 | 🔴 | 拆 `use-workbench.ts` 为多 store + 单一剧集轮询器 | `use-workbench.ts` 全文 |
| 8 | P1 | ✅ | worker 结构化日志 + readiness probe + 队列 `attempts/backoff` | `task-queue.shared.ts` / `task-worker.processor.ts` / `worker.ts` |
| 9 | P1 | 🆕 | Registry 的 `markFailed/markCanceled/execute` 对未知领域改抛错或显式失败 | `task-domain.registry.ts` |
| 10 | P2 | 🔴 | 全局 `running` 改 per-task-type 计数 | `use-workbench.ts` running 字段 |
| 11 | P2 | 🔴 | 真乐观更新（先写本地+pending，失败回滚） | `updateField` / gen* pending 时序 |
| 12 | P2 | 🔴 | 轮询超时持久 banner + 任务中心 | 各 batch 超时分支 |
| 13 | P2 | 🔴 | `StorageService` 换 AWS SDK + s3 模式去无条件本地缓存 | `storage.service.ts` |
| 14 | P2 | 🔴 | 合并两套 API client | `lib/api.ts` / `lib/canvas/api/client.ts` |
| 15 | P2 | ⚠️ | env 收敛：`S3_REGION` s3 必填、`DATABASE_URL` 去默认凭据、已知键 `.strict()` | `config/env.ts` |
| 16 | P2 | 🆕 | `VideoMergeTaskHandler.retry` 去重复 `parseJsonValue` 调用 + 类型守卫 | `video-merge-task.handler.ts:retry` |
| 17 | P2 | 🆕 | Handler 多写操作包 DB 事务 | `base-task-domain.handler.ts` |
| 18 | P2 | 🔴 | 画布 ↔ 短剧主链路集成边界 | `lib/canvas/types` + 文档 |
| 19 | P3 | 🔴 | 删 `.stage-subnav` 死 UI、修重复 `char-count` span | `episode-shell.css` / `script-panel.tsx` |
| 20 | P3 | 🔴 | 统一 nextNav/goContinue/isDone/pipelineProgress 为单一来源 | `use-workbench.ts` / `page.tsx` |
| 21 | P3 | 🔴 | `tasks.controller` HTTP 语义：`NotFoundException` 而非 200+error、`BadRequestException` 而非裸 Error | `tasks.controller.ts:74,158,184` |

---

## 六、总体评价

**这一轮重构和收口是"对的一次"**：它没有在债面上贴补丁，而是动了结构——把 tasks 模块最痛的 5×5 复制换成策略注册表，修了 attemptCount、webhook 幂等、image upsert 竞态、listTasks 全量分页、worker 基础可运维性，并把 CI、脚本校验、后端容器、runtime smoke、统一 release gate 接上。前端把 toast 语义、实体卡片状态、任务恢复和重试体验也明显做实。

**但前端的根本矛盾仍未解决**：`use-workbench.ts` 已约 2053 行，任务恢复能力越强，单 store 越像"所有交互状态的总线"。短期体验变好，但下一次想继续优化性能、做并发任务中心或跨页任务恢复时，拆 store 的成本会比现在更高。

**给下一轮（0.23）的三条建议：**

1. **趁热打铁，把 taskTrackingStore 拆出来。** 本轮的 `isActiveWorkbenchTask` / `entityTasks` / `pendingX` / `genXxx` 轮询逻辑已经高度内聚，正好是拆分单元。拆完顺手把 N 条轮询收敛成单一 `useEpisodePolling`，一次解决 P1-7 和 P1-8 两条。
2. **把后端剩余小漂移收掉**：TTS handler 的 `errorKind` 硬编码、registry 对未知领域的静默 no-op、handler 多写无事务、HTTP 错误语义。这些是本轮重构"搬过来没顺手清理"的点，成本低、收益清晰。
3. **把 release gate 变成长期制度**：保持 `release:verify:no-runtime` 作为 PR 门禁，`release:verify`/`runtime:verify` 作为合并前或发布前门禁；后续再逐步把 Docker runtime smoke 中可 CI 化的部分迁入 Actions。

---

*报告生成时间：2026-07-05（第二轮复审 + 收口更新）｜基线版本：0.22.0｜评审方法：git diff 全量走读 + 新增文件逐行阅读 + release/runtime gate 验证*
